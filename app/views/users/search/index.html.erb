<center>
	<h1><%= facade.keyword.capitalize %> Events</h1>
	<% facade.events.each do |event| %>
	<li><%= event.date %> | <%= event.name %> at <%= event.venue %> <%= link_to "Join Room", "#" %> </li>
	<% end %>
</center>